# Normalize line endings; Windows batch files MUST be CRLF (cmd.exe
# mis-parses LF-only .bat: parenthesized blocks and REM lines break),
# POSIX shell scripts MUST be LF.
* text=auto
*.bat text eol=crlf
*.ps1 text eol=crlf
*.sh  text eol=lf
